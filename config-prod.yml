spring:
  profiles:
    active: pro
---
spring:
  profiles: dev
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${eureka.instance.hostname}:${server.port}
    hostname: localhost
    ip-address: 127.0.0.1
#  instance:
#    hostname: order
spring:
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
  application:
    name: order
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.0.106:3306/springcloud_sell?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    type: com.zaxxer.hikari.HikariDataSource
    password: root
    username: root

PRODUCT:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule
---
spring:
  profiles: test
server:
  port: 8085
---
spring:
  profiles: pro
server:
  port: 8006
